{
  "name": "jarvis-installer-unofficial",
  "version": "1.2.14",
  "private": true,
  "description": "Unofficial cross-platform installer and control panel for Jarvis",
  "main": "dist-electron/main.js",
  "homepage": "https://github.com/Yperbu9474/jarvis-installer-unofficial",
  "author": {
    "name": "Codex",
    "email": "codex@example.com"
  },
  "license": "MIT",
  "bin": {
    "jarv": "./dist-cli/jarv.js"
  },
  "scripts": {
    "dev": "concurrently -k \"vite\" \"tsup electron/main.ts electron/preload.ts --format cjs --out-dir dist-electron --external electron --watch\" \"wait-on tcp:5173 file:dist-electron/main.js && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build:web": "vite build",
    "build:electron": "tsup electron/main.ts electron/preload.ts --format cjs --out-dir dist-electron --external electron",
    "build": "npm run build:web && npm run build:electron",
    "dist": "npm run build && electron-builder --publish never",
    "dist:win": "npm run build && electron-builder --win nsis --publish never",
    "dist:mac": "npm run build && electron-builder --mac dmg --publish never",
    "dist:linux": "npm run build && electron-builder --linux AppImage deb --publish never",
    "typecheck": "tsc --noEmit",
    "build:cli": "tsc -p tsconfig.cli.json",
    "build:all": "npm run build && npm run build:cli"
  },
  "build": {
    "afterPack": "./scripts/after-pack.js",
    "appId": "dev.usejarvis.installer.unofficial",
    "productName": "Jarvis Installer",
    "artifactName": "Jarvis-Installer-${version}-${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "dist-electron/**",
      "package.json"
    ],
    "asar": true,
    "mac": {
      "artifactName": "Jarvis-Installer-macOS-${version}-${arch}.${ext}",
      "category": "public.app-category.developer-tools",
      "target": [
        "dmg"
      ]
    },
    "win": {
      "artifactName": "Jarvis-Installer-Windows-${version}.${ext}",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "linux": {
      "artifactName": "Jarvis-Installer-Linux-${version}.${ext}",
      "executableName": "jarvis-installer-unofficial",
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Development",
      "maintainer": "Codex <codex@example.com>"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Yperbu9474",
        "repo": "jarvis-installer-unofficial"
      }
    ],
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "electronUpdaterCompatibility": ">=2.16"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "electron-log": "^5.2.4",
    "electron-updater": "^6.8.3",
    "node-pty": "^1.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "@types/react": "^19.0.12",
    "@types/react-dom": "^19.0.5",
    "@vitejs/plugin-react": "^4.4.1",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^35.1.4",
    "electron-builder": "^25.1.8",
    "tsup": "^8.4.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.2",
    "wait-on": "^8.0.3"
  },
  "optionalDependencies": {
    "dmg-license": "^1.0.11"
  }
}
